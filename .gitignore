# Dépendances
node_modules/

# Build
site/dist/
site/.astro/

# Système
.DS_Store

# Vidéos de travail à la racine (captures d'écran, exemples de référence).
# La vidéo du process, elle, est un asset du site : site/public/video/.
/*.mov
/*.mp4

# Le projet vidéo (Remotion) vit à côté, il n'appartient pas au dépôt du
# site : ses dépendances pèsent à elles seules plus d'un demi-gigaoctet.
# Les sauvegardes de la vidéo du process y sont aussi, avec leur note.
video/
